services:
  oura-reader:
    build: .
    restart: unless-stopped
    ports:
      - "0.0.0.0:8080:8080"
    volumes:
      - oura-data:/data
      - ./secrets:/run/secrets:ro
    environment:
      - OURA_LISTEN_ADDR=0.0.0.0:8080
      - OURA_FETCH_INTERVAL=6h

volumes:
  oura-data:
